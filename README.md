# next-employer
Project using next.js for check Employer API and struturing project.

1.After cloning the repository, run the command:
    `npm install`
    This will install all the necessary dependencies.

2.To start the development server, navigate into the project directory and run:
    `npm run dev`
